# RAG orchestration. Engineering retrieval/evaluation remains deterministic;
# only the optional /answer synthesis sends grounded evidence to DeepSeek.
langchain>=0.3.0
langchain-community>=0.3.0
langchain-deepseek>=0.1.0
langchain-huggingface>=0.1.0
openai>=1.0.0,<2.0.0

# Vector stores and embeddings
# Engineering RAG supports a distributed Milvus backend and a local FAISS
# backend.  The default remains FAISS so a new clone works without a server.
pymilvus>=2.5.0
faiss-cpu>=1.8.0
sentence-transformers>=3.0.0
transformers>=4.40.0
torch>=2.0.0

# Document and source ingestion
pypdf>=4.0.0
pymupdf>=1.24.0
openpyxl>=3.1.0
unstructured>=0.15.0
markdown>=3.5
PyYAML>=6.0

# Evaluation
# ragas/datasets are retained for the legacy evaluator. Formal engineering
# evaluation uses rag_core/evaluation/engineering.py and source-level metrics.
ragas>=0.2.0
datasets>=2.19.0
pandas>=2.2.0
numpy>=1.26.0

# API and local static UI
fastapi>=0.115.0
uvicorn>=0.30.0

# Runtime utilities
python-dotenv>=1.0.0
pydantic>=2.0.0
tqdm>=4.66.0
loguru>=0.7.0

# Tests
pytest>=8.0.0
pytest-asyncio>=0.23.0
httpx>=0.27.0
